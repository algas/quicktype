name: haskell
version: 0.0.1
category: Data
dependencies:
  - base >= 4.7 && < 5
  - aeson
  - bytestring
  - lens
  - lens-aeson
  - text

executables:
  haskell:
    source-dirs: .
    main: Main.hs
